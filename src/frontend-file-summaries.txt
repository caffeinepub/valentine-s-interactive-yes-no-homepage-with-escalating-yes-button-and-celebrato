{
  "frontend/src/App.tsx": "Main Valentine's homepage component with interactive Yes/No buttons, two couple photos displayed side-by-side (IMG_20260208_030749-3.jpg as the main photo with hover-activated image picker overlay, and image-1.jpg as a secondary photo), robust client-side image preview with fallback placeholder when default image fails to load, accessible inline error messaging, and the headline \"Annammooo!! Will you be my valentine????\" with enthusiastic punctuation.",
  "frontend/src/components/ValentineModal.tsx": "Modal component that displays the celebration image with hover-activated image picker overlay using production-safe asset URL for the default valentine popup illustration (valentine-popup-illustration.dim_1024x1024.png), robust client-side image preview with fallback placeholder when default image fails to load, accessible inline error messaging, romantic message when Yes is clicked, and backdrop click and Escape key dismissal support.",
  "frontend/src/index.css": "Global styles with Valentine-themed OKLCH color system featuring warm reds, pinks, and creams for both light and dark modes, plus custom animation utilities.",
  "frontend/tailwind.config.js": "Tailwind configuration with extended Valentine theme colors and custom border radius settings.",
  "frontend/src/hooks/useClientImagePreview.ts": "Custom React hook that handles client-side image file selection with robust preview management, stale-read protection, resource cleanup, last-known-good preview tracking, enhanced error handling that distinguishes between default asset failures and user-selected image failures, and a shouldShowImage flag to control fallback rendering.",
  "frontend/src/utils/assetUrl.ts": "Utility function that builds deployment-safe public asset URLs using Vite's BASE_URL to ensure static assets resolve correctly in production deployments with non-root base paths."
}